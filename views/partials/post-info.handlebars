<article class="card">
  <div class="card-header flex-row justify-space-between">
    <h2>
        {{title}}
      </a>
    </h2>
    <div>
      posted by
      {{user.username}}
      {{!-- {{format_date createdAt}} --}}
    </div>
  </div>
  <div class="card-body">
    {{post_body}}
  </div>
</article>